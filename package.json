{
  "name": "issue-286",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:tsdown": "tsdown --entry src/dom/dom.ts --entry src/node/node.ts --outDir dist/dist_tsdown --format esm --dts",
    "build:tsc": "tsc --build .",
    "test": "rm -rf dist && pnpm run build:tsdown",
    "test2": "rm -rf dist && pnpm run build:tsc && pnpm run build:tsdown"
  },
  "devDependencies": {
    "@types/node": "22.15.29",
    "tsdown": "file:~/.config/packlink/tsdown-0.12.5-1748760041882.tgz",
    "typescript": "5.8.3"
  },
  "pnpm": {},
  "dependencies": {
    "rolldown-plugin-dts": "file:~/.config/packlink/rolldown-plugin-dts-0.13.7-1748768175379.tgz"
  }
}